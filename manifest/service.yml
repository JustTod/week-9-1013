apiVersion: v1
kind: Service
metadata:
  name: simple-node-app-tod-service
  namespace: group-4
spec:
  selector:
    app: simple-node-app-tod-deployment
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  type: ClusterIP
