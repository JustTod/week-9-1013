apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-node-app-tod-deployment
  namespace: group-4
  labels:
    app: simple-node-app-tod-deployment
spec:
  replicas: 1
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 50%
  selector:
    matchLabels:
      app: simple-node-app-tod-deployment
  template:
    metadata:
      labels:
        app: simple-node-app-tod-deployment
    spec:
      containers:
        - name: simple-node-app-tod
          image: justtod/simple-node-app
          ports:
            - containerPort: 3000
